# ============================================================
# 顶层依赖 — pip 自动解析子依赖版本
# 本地开发用 pip freeze > requirements-lock.txt 锁定
# ============================================================

# Web 框架
fastapi>=0.100.0
uvicorn[standard]

# LangChain 生态
langchain>=1.3.0
langchain-community>=0.4.0
langchain-chroma
langchain-text-splitters

# 向量数据库
chromadb>=1.5.0

# LLM
openai>=2.0.0

# Embedding 模型
sentence-transformers

# 文档解析
pypdf
docx2txt

# HTTP
httpx

# 混合检索
rank-bm25

# 对话历史持久化
redis>=5.0.0

# 数据校验
pydantic>=2.0.0

# Dev 依赖
pytest>=9.0.0
pytest-asyncio>=0.23.0
pytest-cov>=5.0.0
ruff>=0.15.0
